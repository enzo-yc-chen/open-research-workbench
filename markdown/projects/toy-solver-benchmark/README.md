# Toy Solver Benchmark

This fictional project demonstrates the project directory shape.

Start with [status.md](status.md). Use `evidence/` for stable support,
`analysis/` for evolving reasoning, and `outputs/plan.md` for the first output
plan.
