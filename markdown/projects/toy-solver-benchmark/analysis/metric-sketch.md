# Metric Sketch

## Purpose

Show where dynamic project reasoning belongs.

## Candidate Metric

For a real toy benchmark, a first metric might compare expected and observed
outputs on a tiny public input. This template does not define a real benchmark.

## Failure Interpretation

If no source defines a reproducible input and expected output, do not create an
execution benchmark task yet. Keep the project in material-gathering mode.
